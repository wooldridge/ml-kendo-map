{
  "name": "ml-kendo-map",
  "version": "1.0.0",
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "(cd setup && npm install); (cd server && npm install); (cd ui && npm install);"
  }
}
